#!/bin/sh
# Author-provided question preparation, not a Waveform discovery API.
# Replace this example with whatever processing the starter needs: inspect
# earlier answers, read local config, or fetch available providers from an API.
# STARTER_VAR_WAVEFORM contains the earlier resolved answer.
# Return a JSON array on stdout; send progress/diagnostics to stderr.
# No interactive input here, so the same preparation works during updates.
printf '%s\n' '["google"]'
