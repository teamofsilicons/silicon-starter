## Waveform

Waveform provides text-to-speech and speech-to-text.
Use `waveform --help` for its available commands.
The configured default TTS provider is {{ var.waveform_tts_provider }}.

Use voice when requested or when it suits the conversation. Use Briefcase to
share generated audio where a tool expects a file link.
